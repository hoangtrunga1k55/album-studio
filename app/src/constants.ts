/** dataTransfer key for dragging an imported image (by id) from the panel onto a slot. */
export const IMAGE_DND_KEY = "application/x-image-id";

/** dataTransfer key for dragging a typo design (by id) onto the canvas. */
export const TYPO_DND_KEY = "application/x-typo-id";
